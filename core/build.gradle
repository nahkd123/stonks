dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.9.1'
    api 'com.github.nahkd123.nahara-toolkit:nahara-common-tasks:main-SNAPSHOT'
}

tasks.named('test') {
    useJUnitPlatform()
}
